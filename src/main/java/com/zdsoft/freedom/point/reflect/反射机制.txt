1.何谓反射机制
JAVA反射机制是在运行状态中，对于任意一个类，都能够知道这个类的所有属性和方法；
对于任意一个对象，都能够调用它的任意一个方法；
这种动态获取的信息以及动态调用对象的方法的功能称为java语言的反射机制。

2.反射机制如何实现
谈到反射机制，最诱人的莫过于“动态”二字了。

其实这里的两个动态是一个意思，都指的是非编译时处理，抑或运行时处理。
这种机制，可以让程序的弹性增加不少，因为借由此机制，
客户可以在程序运行时改变一些他关心的性质：分配内存（当然他可能 完全不知道这么做了），调用某个类（当然他还是被蒙在鼓里）等。

version1
